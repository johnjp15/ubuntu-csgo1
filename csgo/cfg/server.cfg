//default server settings
//jp
//08-07-2016

//this file is executed at the start of every map, whereas autoexec is only executed at the start of the first map

hostname "jp's server | 64 tick"
sv_tags "jp, csgo, 64, practice"
sv_password ""
mp_warmuptime "60"
//rcon password executable here, ignored by git
//exec rcon_password_file

//banned users and ip
exec banned_user
exec banned_ip

writeid
writeip

echo "*********server.cfg executed*************"
